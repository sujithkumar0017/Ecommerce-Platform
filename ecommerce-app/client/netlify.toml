# Netlify config for the ShopKart storefront SPA.
# In Netlify, set this site's "Base directory" to: ecommerce-app/client
# (command/publish below are relative to that base).

[build]
  command = "npm run build"
  publish = "dist"

  [build.environment]
    # Baked into the build at compile time. Point at the ecommerce API on Render.
    # Change here (or override in Netlify UI → Site settings → Environment) if the
    # Render URL differs, then trigger a redeploy.
    VITE_API_URL = "https://shopkart-ecommerce-api.onrender.com/api"

# SPA fallback so client-side routes (e.g. /products/1) work on refresh.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200




